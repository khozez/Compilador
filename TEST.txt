{
    SHORT x,
    SHORT y,
    FLOAT z,
    FLOAT aux,

    x = 4_s,
    y = 5_s,
    z = x + 7.0,
    PRINT %variable mayor a
    8%,

    IF (x>=4_s)
    {
        aux = x + 4.0,
        IF (aux > 8.0)
            z = 9.0,
        END_IF,
        z = aux,
    }ELSE
    {
        aux = 0.0,
        z = 8.0,
    }END_IF,

    WHILE (z !! 10.0) DO
    {
        aux = 1_s,
        z = z + aux,
    },


}